% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg_sankey.R
\name{make_sankey}
\alias{make_sankey}
\title{Create Sankey diagram}
\usage{
make_sankey(
  dat,
  node_labels = NULL,
  flow_labels = currentsee::flow_labels,
  colors = currentsee::colours_df,
  node_width = 0.05,
  node_label_font_size = 3,
  flow_label_font_size = 2.5,
  x_label_font_size = 12,
  x_names = make_x_labs(names(dat)),
  flow_alpha = 0.3,
  gradient_res = 3000
)
}
\arguments{
\item{dat}{Data frame where each column represents a step and each row represents a flow path}

\item{node_labels}{Data frame with columns 'node' and 'label' for custom node labels (optional)}

\item{flow_labels}{Data frame with columns 'flow_start', 'flow_end', 'flow_label' for custom flow labels (optional)}

\item{colors}{Data frame with columns 'node' and 'colour' for custom node colors, or named vector (optional)}

\item{node_width}{Numeric. Width of node rectangles (default: 0.05)}

\item{node_label_font_size}{Numeric. Font size for node labels (default: 3)}

\item{flow_label_font_size}{Numeric. Font size for flow labels (default: 2.5)}

\item{x_label_font_size}{Numeric. Font size for x-axis tick labels}

\item{x_names}{Character vector. Labels for x-axis (column names by default)}

\item{flow_alpha}{Numeric. Transparency of flow ribbons (default: 0.3)}

\item{gradient_res}{Integer. Resolution of flow gradients for smoothness (default: 3000)}
}
\value{
ggplot2 object representing the Sankey diagram
}
\description{
Generates a Sankey diagram with customizable nodes, flows, colors, and labels.
Supports dead-end flows (NA values), custom labeling, and flexible styling options.
}
